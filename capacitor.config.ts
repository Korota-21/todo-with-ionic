import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.marwa.to-do',
  appName: 'to-do',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
